# 프론트엔드 실행 가이드

## 문제 해결 단계

### 1단계: npm 권한 문제 해결 (터미널에서 실행)
```bash
sudo chown -R $(whoami) ~/.npm
```
비밀번호를 입력하세요.

### 2단계: 프론트엔드 의존성 설치
```bash
cd /Users/jace/Downloads/goldentime-llm/frontend
npm install
```

### 3단계: 프론트엔드 서버 실행
```bash
npm run dev
```

서버가 시작되면 다음과 같은 메시지가 표시됩니다:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

### 4단계: 브라우저에서 접속
```
http://localhost:5173
```

## 백엔드도 함께 실행해야 하는 경우

프론트엔드만 실행해도 기본 화면은 보이지만, 실제 데이터를 보려면 백엔드도 실행해야 합니다.

### 백엔드 실행 (새 터미널 창)
```bash
cd /Users/jace/Downloads/goldentime-llm
npm install  # 백엔드 의존성 설치 (처음 한 번만)
npm run dev  # 또는 npm start
```

## 빠른 실행 (한 번에)

터미널 1 (백엔드):
```bash
cd /Users/jace/Downloads/goldentime-llm
npm run dev
```

터미널 2 (프론트엔드):
```bash
cd /Users/jace/Downloads/goldentime-llm/frontend
npm run dev
```

## 문제 해결

### 포트가 이미 사용 중인 경우
```bash
# 5173 포트 사용 중인 프로세스 확인
lsof -ti:5173

# 프로세스 종료
kill -9 $(lsof -ti:5173)
```

### npm install이 계속 실패하는 경우
```bash
# npm 캐시 정리
npm cache clean --force

# 다시 설치
npm install
```
