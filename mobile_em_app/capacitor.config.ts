import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.primeplay.goldentime',
  appName: 'GoldenTime Health',
  webDir: 'dist'
};

export default config;
