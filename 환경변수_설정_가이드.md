# 환경변수 설정 가이드

## 문제 원인
백엔드 서버가 시작되지 않는 이유는 `.env` 파일이 없어서 필수 환경변수(`MONGODB_URI`, `JWT_SECRET`)가 설정되지 않았기 때문입니다.

## 해결 방법

### 1단계: .env 파일 생성

프로젝트 루트 디렉토리(`/Users/jace/Downloads/goldentime-llm`)에 `.env` 파일을 생성하세요.

터미널에서 실행:
```bash
cd /Users/jace/Downloads/goldentime-llm
cp config/env.example .env
```

또는 직접 생성:
```bash
cd /Users/jace/Downloads/goldentime-llm
cat > .env << 'EOF'
NODE_ENV=development
PORT=3000

MONGODB_URI=mongodb://localhost:27017/goldentime

JWT_SECRET=goldentime-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Zepp 개발자 콘솔(앱 등록 후 발급)
ZEPP_CLIENT_ID=
ZEPP_CLIENT_SECRET=
ZEPP_REDIRECT_URI=http://localhost:3000/api/zepp/oauth/callback
ZEPP_API_BASE_URL=

# Ollama (로컬)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b

# Ollama 사용(있으면 true)
ENABLE_OLLAMA=false

# 파인튜닝된 모델 사용 (true로 설정 시 파인튜닝된 모델 사용)
USE_FINETUNED_MODEL=false
OLLAMA_FINETUNED_MODEL=goldentime-emergency:latest

# 이메일 알림 설정 (Nodemailer)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=noreply@goldentime.com

# SMS 알림 설정 (알리고 API - 선택사항)
ALIGO_API_KEY=
ALIGO_USER_ID=
ALIGO_SENDER_PHONE=

# OSRM 라우팅 서버 (기본값: 공개 OSRM 서버)
OSRM_BASE_URL=http://router.project-osrm.org
EOF
```

### 2단계: MongoDB 실행 확인

```bash
# MongoDB 실행 확인
brew services list | grep mongodb

# MongoDB가 실행되지 않았다면 시작
brew services start mongodb-community
```

### 3단계: 백엔드 서버 실행

**새 터미널 창**에서:
```bash
cd /Users/jace/Downloads/goldentime-llm
npm run dev
```

서버가 정상적으로 시작되면 다음과 같은 메시지가 표시됩니다:
```
✅ 환경변수 검증 완료
MongoDB Connected: localhost
Server running on port 3000
```

### 4단계: 로그인 테스트

브라우저에서 `http://localhost:5173` 접속 후:
- 이메일: `controller@test.com`
- 비밀번호: `test1234`

## 빠른 실행 (한 번에)

```bash
# 1. .env 파일 생성
cd /Users/jace/Downloads/goldentime-llm
cp config/env.example .env

# 2. MongoDB 시작 (필요한 경우)
brew services start mongodb-community

# 3. 백엔드 서버 실행
npm run dev
```

## 확인 사항

- [ ] `.env` 파일이 프로젝트 루트에 생성되었는가?
- [ ] MongoDB가 실행 중인가?
- [ ] 백엔드 서버가 포트 3000에서 실행 중인가?
- [ ] 프론트엔드 서버가 포트 5173에서 실행 중인가?
