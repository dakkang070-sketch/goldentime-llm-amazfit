# 로그인 실패 해결 가이드

## 현재 문제
- ❌ 백엔드 서버가 실행되지 않음
- ❌ .env 파일이 없음
- ✅ 컨트롤러 계정은 존재함

## 해결 방법

### 방법 1: 자동 시작 스크립트 사용 (권장)

터미널에서 다음 명령어 실행:

```bash
cd /Users/jace/Downloads/goldentime-llm
bash start-backend.sh
```

이 스크립트가 자동으로:
1. .env 파일 생성
2. MongoDB 실행 확인
3. 컨트롤러 계정 확인
4. 백엔드 서버 시작

### 방법 2: 수동 설정

#### 1단계: .env 파일 생성

```bash
cd /Users/jace/Downloads/goldentime-llm
cp config/env.example .env
```

#### 2단계: MongoDB 시작

```bash
brew services start mongodb-community
```

#### 3단계: 백엔드 서버 실행

**새 터미널 창**에서:
```bash
cd /Users/jace/Downloads/goldentime-llm
npm run dev
```

서버가 정상적으로 시작되면:
```
✅ 환경변수 검증 완료
MongoDB Connected: localhost
Server running on port 3000
```

#### 4단계: 로그인 테스트

브라우저에서 `http://localhost:5173` 접속:
- 이메일: `controller@test.com`
- 비밀번호: `test1234`

## 문제 진단

### 백엔드 서버가 시작되지 않는 경우

**터미널에서 확인:**
```bash
# 포트 3000 확인
lsof -ti:3000

# .env 파일 확인
ls -la .env

# 환경변수 확인
node -e "require('dotenv').config(); console.log('MONGODB_URI:', process.env.MONGODB_URI);"
```

### 로그인이 여전히 실패하는 경우

**브라우저 개발자 도구 확인:**
1. F12 또는 Cmd+Option+I로 개발자 도구 열기
2. **Console 탭**에서 에러 메시지 확인
3. **Network 탭**에서 `/api/controllers/login` 요청 확인
   - 요청이 보이지 않음 → 프론트엔드가 백엔드에 연결하지 못함
   - 401 에러 → 계정 정보 문제
   - 500 에러 → 서버 내부 오류
   - Connection refused → 백엔드 서버가 실행되지 않음

### API 직접 테스트

터미널에서:
```bash
curl -X POST http://localhost:3000/api/controllers/login \
  -H "Content-Type: application/json" \
  -d '{"email":"controller@test.com","password":"test1234"}'
```

성공하면:
```json
{
  "success": true,
  "message": "로그인 성공",
  "token": "...",
  "controller": {...}
}
```

실패하면:
```json
{
  "success": false,
  "message": "이메일 또는 비밀번호가 올바르지 않습니다."
}
```

## 체크리스트

- [ ] .env 파일이 프로젝트 루트에 있는가?
- [ ] MongoDB가 실행 중인가?
- [ ] 백엔드 서버가 포트 3000에서 실행 중인가?
- [ ] 프론트엔드 서버가 포트 5173에서 실행 중인가?
- [ ] 브라우저 콘솔에 에러가 없는가?
- [ ] Network 탭에서 로그인 요청이 성공하는가?

## 빠른 실행 (한 번에)

```bash
# 터미널 1: 백엔드
cd /Users/jace/Downloads/goldentime-llm
cp config/env.example .env
brew services start mongodb-community
npm run dev

# 터미널 2: 프론트엔드 (이미 실행 중이면 생략)
cd /Users/jace/Downloads/goldentime-llm/frontend
npm run dev
```

## 추가 도움

문제가 계속되면 다음 정보를 확인해주세요:
1. 백엔드 서버 실행 시 터미널 출력
2. 브라우저 개발자 도구의 Console 에러
3. 브라우저 개발자 도구의 Network 탭에서 `/api/controllers/login` 요청 상태
