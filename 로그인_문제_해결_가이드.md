# 로그인 문제 해결 가이드

## 현재 상태
- ✅ 프론트엔드 서버: 실행 중 (http://localhost:5173)
- ✅ 컨트롤러 계정: 생성 완료 (controller@test.com / test1234)
- ❌ 백엔드 서버: 실행되지 않음

## 해결 방법

### 1단계: MongoDB 실행 확인 및 시작

터미널에서 다음 명령어로 MongoDB 상태 확인:
```bash
# MongoDB 실행 확인
brew services list | grep mongodb

# MongoDB가 실행되지 않았다면 시작
brew services start mongodb-community

# 또는 직접 실행
mongod
```

### 2단계: 백엔드 서버 실행

**새 터미널 창**을 열고 다음 명령어 실행:

```bash
cd /Users/jace/Downloads/goldentime-llm
npm run dev
```

또는

```bash
cd /Users/jace/Downloads/goldentime-llm
node backend/server.js
```

### 3단계: 서버 실행 확인

백엔드 서버가 정상적으로 시작되면 다음과 같은 메시지가 표시됩니다:
```
MongoDB Connected: localhost
Server running on port 3000
```

### 4단계: 브라우저에서 로그인

1. 브라우저에서 `http://localhost:5173` 접속
2. 로그인 화면에서:
   - 이메일: `controller@test.com`
   - 비밀번호: `test1234`
3. 로그인 버튼 클릭

## 문제 진단

### 백엔드 서버가 시작되지 않는 경우

**에러 메시지 확인:**
- `MongoDB 연결 실패`: MongoDB가 실행되지 않음 → 1단계 참고
- `포트 3000 이미 사용 중`: 다른 프로세스가 포트를 사용 중
  ```bash
  # 포트 사용 중인 프로세스 확인
  lsof -ti:3000
  
  # 프로세스 종료
  kill -9 $(lsof -ti:3000)
  ```

### 로그인이 여전히 실패하는 경우

**브라우저 개발자 도구 확인:**
1. 브라우저에서 F12 또는 Cmd+Option+I (Mac)로 개발자 도구 열기
2. Console 탭에서 에러 메시지 확인
3. Network 탭에서 `/api/controllers/login` 요청 확인
   - 요청이 실패하면: 백엔드 서버가 실행되지 않음
   - 401 에러: 계정 정보가 잘못됨
   - 500 에러: 서버 내부 오류

## 빠른 실행 스크립트

터미널에서 다음을 실행:

```bash
# MongoDB 시작 (필요한 경우)
brew services start mongodb-community

# 백엔드 서버 실행
cd /Users/jace/Downloads/goldentime-llm
npm run dev
```

## 확인 사항 체크리스트

- [ ] MongoDB가 실행 중인가?
- [ ] 백엔드 서버가 포트 3000에서 실행 중인가?
- [ ] 프론트엔드 서버가 포트 5173에서 실행 중인가?
- [ ] 브라우저 콘솔에 에러가 있는가?
- [ ] 네트워크 요청이 성공하는가?

## 추가 도움

문제가 계속되면 다음 정보를 확인해주세요:
1. 백엔드 서버 실행 시 에러 메시지
2. 브라우저 콘솔의 에러 메시지
3. Network 탭의 `/api/controllers/login` 요청 상태
